// Tailwind CSS v4 + Next.js: use the dedicated PostCSS plugin.
// (In the Vite version this was handled by the @tailwindcss/vite plugin.)
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
